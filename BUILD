subinclude("//build_defs:js")

# The editor's view of the build graph: run it, then extend tsconfig.plz.json
# from the repo tsconfig. See js_project's docstring.
js_project(
    name = "js-project",
)
