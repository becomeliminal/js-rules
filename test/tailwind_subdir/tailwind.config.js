/** @type {import('tailwindcss').Config} */
module.exports = {
  // Relative path — only works if Tailwind's CWD is this config's directory
  content: ["./src/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
}
